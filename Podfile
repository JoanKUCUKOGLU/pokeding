# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'listedepokemon' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for listedepokemon
	pod 'Alamofire' , '~> 4.0'
	pod 'SwiftLint'
	pod 'SwiftyJSON' , '~> 4.0'

  target 'listedepokemonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'listedepokemonUITests' do
    # Pods for testing
  end

end
